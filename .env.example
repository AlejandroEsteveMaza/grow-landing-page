# Local builds may omit all three Sanity values and use local content.
# The project ID and dataset are configuration; the API token is a secret.
DEPLOY_ENV=local
SANITY_PROJECT_ID=
SANITY_DATASET=
SANITY_API_TOKEN=
