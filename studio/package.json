{
  "name": "tunorte-sanity-studio",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy:production:dry-run": "DEPLOY_ENV=production SANITY_STUDIO_BASEPATH=/ sanity deploy --dry-run --url tunorte.sanity.studio --title \"TuNorte Content Studio\" --schema-required",
    "deploy:production": "DEPLOY_ENV=production SANITY_STUDIO_BASEPATH=/ sh -c 'printf \"Type DEPLOY TUNORTE PRODUCTION to continue: \"; IFS= read -r confirmation && [ \"$confirmation\" = \"DEPLOY TUNORTE PRODUCTION\" ] || { printf \"Deployment cancelled.\\n\" >&2; exit 1; }; exec sanity deploy --url tunorte.sanity.studio --title \"TuNorte Content Studio\" --schema-required'"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "sanity": "^6.4.0",
    "styled-components": "^6.4.3"
  }
}
